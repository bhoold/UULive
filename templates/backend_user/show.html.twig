{% extends 'base.html.twig' %}

{% block title %}BackendUser{% endblock %}

{% block body %}
    <h1>BackendUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ backend_user.id }}</td>
            </tr>
            <tr>
                <th>Uuid</th>
                <td>{{ backend_user.uuid }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ backend_user.roles ? backend_user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ backend_user.password }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ backend_user.username }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_user_index') }}">back to list</a>

    <a href="{{ path('backend_user_edit', {'id': backend_user.id}) }}">edit</a>

    {{ include('backend_user/_delete_form.html.twig') }}
{% endblock %}
